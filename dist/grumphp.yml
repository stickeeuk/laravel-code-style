parameters:
    git_dir: .
    hooks_dir: null
    bin_dir: vendor/bin
    tasks: { git_blacklist: { keywords: [dd(, var_dump(, die(] }, phpcsfixer2: null, composer: null, phpstan: { configuration: vendor/stickee/laravel-code-style/dist/phpstan.neon } }
